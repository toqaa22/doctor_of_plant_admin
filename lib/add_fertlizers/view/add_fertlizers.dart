import 'package:flutter/material.dart';

class AddFertlizers extends StatelessWidget {
  const AddFertlizers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
